# Large source data
data/real/
supplementary/
reference/*.gtf.gz

# draft_sequence: ignore large source/intermediate files, keep hub assets
draft_sequence/
!draft_sequence/HRP.bigPsl.bb
!draft_sequence/bigPsl.as

# ucsc_hub_internal: no-HTML version for internal UCSC use, not for public repo
ucsc_hub_internal/

# nersc_upload: local data manifest for NERSC upload, not for public repo
nersc_upload/

# bigPsl hosted on GCS — too large for GitHub
ucsc_hub/hg38_polyA-RNA/HRP.bigPsl.bb
draft_sequence/HRP.bigPsl.bb

# vcfs: large local copies (variants hosted externally via GCS)
vcfs/

# Downloaded binaries
utils/pslToBigPsl

# macOS
.DS_Store
